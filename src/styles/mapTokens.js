import { tokens } from "./tokens";

export const mapTokens = {
  colors: {
    default: tokens.colors.disabled,
    selected: tokens.colors.accent,
  },
  weights: {
    default: 2,
    selected: 4,
  },
  opacities: {
    default: 0.6,
    selected: 0.8,
  },
};
